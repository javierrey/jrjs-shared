// index-scripts/index.html.js
// @ts-check

import {
  ge, gt, qs, qa,
} from './lib/view/view-x.js';

/* * */

(function ready(...args) {
  if (!document.body) return setTimeout(ready, 50, ...args);
  // ready
})();

/* * */
